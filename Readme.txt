make p1 : will compile rsa.c and aes.c, and execute the respective binaries. It will output the numbers into rsa.txt and aes.txt.

MATLAB was used to plot the numbers, Matlab figures have been included.

Make p3 : will compile and execute client.c, and output the secret code to secret.txt. 

